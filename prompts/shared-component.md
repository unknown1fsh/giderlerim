Create a new shared component for this repository.

Rules:
- Component must be generic and reusable, not feature-specific.
- Component must follow the central UI standard.
- Component must be added to the shared components folder.
- Component must accept standard props/inputs for its purpose.
- Do not hard-code feature-specific logic inside the component.
- Document the component's purpose and props/inputs inline.
